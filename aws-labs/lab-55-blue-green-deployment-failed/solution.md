## Solution: lab-55-blue-green-deployment-failed

### Root Cause
CodeDeploy blue/green: hooks failing, traffic not shifting, rollback

### Category: Migration & Microservices

### Fix
1. Identify the misconfigured component from the error
2. Check the relevant AWS service configuration
3. Apply the fix (update policy/SG/route/config)
4. Verify the error is resolved

### Key Takeaway
CodeDeploy blue/green: hooks failing, traffic not shifting, rollback — this is a common production issue. Document the fix in your runbook.
