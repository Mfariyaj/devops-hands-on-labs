## Solution: lab-32-s3-replication-not-working

### Root Cause
Cross-region replication not replicating: versioning disabled, IAM wrong

### Category: Storage & Database

### Fix
1. Identify the misconfigured component from the error
2. Check the relevant AWS service configuration
3. Apply the fix (update policy/SG/route/config)
4. Verify the error is resolved

### Key Takeaway
Cross-region replication not replicating: versioning disabled, IAM wrong — this is a common production issue. Document the fix in your runbook.
