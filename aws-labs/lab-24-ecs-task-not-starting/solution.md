## Solution: lab-24-ecs-task-not-starting

### Root Cause
ECS task won't start: task role missing permissions, secret can't be fetched

### Category: EKS & Containers

### Fix
1. Identify the misconfigured component from the error
2. Check the relevant AWS service configuration
3. Apply the fix (update policy/SG/route/config)
4. Verify the error is resolved

### Key Takeaway
ECS task won't start: task role missing permissions, secret can't be fetched — this is a common production issue. Document the fix in your runbook.
