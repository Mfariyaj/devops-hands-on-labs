## Solution: lab-26-lambda-memory-oom

### Root Cause
Lambda OOM killed: 128MB memory not enough for image processing

### Category: Serverless

### Fix
1. Identify the misconfigured component from the error
2. Check the relevant AWS service configuration
3. Apply the fix (update policy/SG/route/config)
4. Verify the error is resolved

### Key Takeaway
Lambda OOM killed: 128MB memory not enough for image processing — this is a common production issue. Document the fix in your runbook.
