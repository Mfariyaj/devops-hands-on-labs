## Solution: lab-02-iam-cross-account-assume-role

### Root Cause
Cross-account AssumeRole fails: Trust policy wrong, external ID mismatch

### Category: IAM & Security

### Fix
1. Identify the misconfigured component from the error
2. Check the relevant AWS service configuration
3. Apply the fix (update policy/SG/route/config)
4. Verify the error is resolved

### Key Takeaway
Cross-account AssumeRole fails: Trust policy wrong, external ID mismatch — this is a common production issue. Document the fix in your runbook.
