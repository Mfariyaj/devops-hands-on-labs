## Solution: lab-15-dns-private-hosted-zone

### Root Cause
Private hosted zone DNS not resolving: VPC association missing

### Category: Networking & VPC

### Fix
1. Identify the misconfigured component from the error
2. Check the relevant AWS service configuration
3. Apply the fix (update policy/SG/route/config)
4. Verify the error is resolved

### Key Takeaway
Private hosted zone DNS not resolving: VPC association missing — this is a common production issue. Document the fix in your runbook.
