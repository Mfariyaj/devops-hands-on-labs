## Solution: lab-07-secrets-manager-rotation-failed

### Root Cause
Secrets Manager Lambda rotation function failing: timeout, permissions

### Category: IAM & Security

### Fix
1. Identify the misconfigured component from the error
2. Check the relevant AWS service configuration
3. Apply the fix (update policy/SG/route/config)
4. Verify the error is resolved

### Key Takeaway
Secrets Manager Lambda rotation function failing: timeout, permissions — this is a common production issue. Document the fix in your runbook.
